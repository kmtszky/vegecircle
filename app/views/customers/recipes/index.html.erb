<div class="container">
  <div class="row">
    <div class="col-12 col-md-10 mx-auto">
      <h4 class="mb-4">レシピ一覧</h4>
      <%= render 'searches/recipe_search' %>
      <%= render 'customers/recipes/index', recipes: @recipes %>
      <%= paginate @recipes %>
    </div>
  </div>
</div>