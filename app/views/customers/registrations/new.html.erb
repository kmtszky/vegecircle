<div class="container">
  <div class="row">
    <div class="col-12 col-md-5 mx-auto p-4 text-center sign">

    <h4>新規登録</h4><br>

    <%= form_with model: @customer, url: customer_registration_path, id: 'new_customer', class: 'new_customer', local: true do |f| %>
      <%= render "customers/shared/error_messages", resource: resource %>
      <table class="table table-borderless">
        <tbody>
          <tr>
            <td align="right"><%= f.label :nickname, "ニックネーム" %></td>
            <td align="left"><%= f.text_field :nickname, autofocus: true, autocomplete: "nickname", class: "form-control form-control-sm" %></td>
          </tr>
          <tr>
            <td align="right"><%= f.label :email, "メールアドレス" %></td>
            <td align="left"><%= f.email_field :email, autocomplete: "email", class: "form-control form-control-sm" %></td>
          </tr>
          <tr>
            <td align="right">
              <%= f.label :password, "パスワード" %>
              <% if @minimum_password_length %>
                （<%= @minimum_password_length %> 文字以上）
              <% end %>
              </td>
            <td align="left">
              <%= f.password_field :password, autocomplete: "new-password", class: "form-control form-control-sm" %>
            </td>
          </tr>
          <tr>
            <td align="right"><%= f.label :password_confirmation, "パスワード（確認用）" %></td>
            <td align="left"><%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control form-control-sm" %></td>
          </tr>
          <tr>
            <td colspan="2">
              <%= f.submit "新規登録", class: "btn btn-sm btn-secondary" %>
            </td>
          </tr>
        </tbody>
      </table>
    <% end %>
    <p>登録済みのアカウントからログインされる方は<%= render "customers/shared/session" %></p>
    </div>
  </div>
</div>