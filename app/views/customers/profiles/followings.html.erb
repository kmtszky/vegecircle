<div class="container">
  <div class="row">
    <div class="col-11 col-md-10 mx-auto">
      <h4 class="mb-4">フォローしている農家さん</h4>

      <table class="table table-hover">
        <thead>
          <tr>
            <th></th>
            <th>農家さんのお名前</th>
            <th>直売所の住所</th>
            <th>新着お知らせ</th>
          </tr>
        </thead>
        <tbody>
          <% @followings.each do |farmer| %>
            <tr>
              <td><%= link_to farmer_path(farmer) do %>
                    <%= attachment_image_tag farmer, :farmer_image, :fill, 60, 60, class: "rounded-circle", fallback: asset_path('no_images/no_image_md.png'), size: '60x60' %>
                  <% end %></td>
              <td><%= link_to farmer_path(farmer) do %><%= farmer.name %><% end %></td>
              <td><%= farmer.store_address %></td>
              <td><% if farmer.news.exists? %><%= farmer.news.last.news %><% end %></td>
            </tr>
          <% end %>
          <%= paginate @followings %>
        </tbody>
      </table>
    </div>
  </div>
</div>