<% if current_customer.following?(farmer) %>
  <%= link_to "フォローを外す", farmer_follow_path(farmer, follow), method: :delete, remote: true, class: "btn btn-sm btn-danger" %>
<% else %>
  <%= link_to "フォローする", farmer_follows_path(farmer), method: :post, remote: true, class: "btn btn-sm btn-success" %>
<% end %>
