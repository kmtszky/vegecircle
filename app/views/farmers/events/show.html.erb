<div class="container">
  <div class="row">
    <div class="col-12 col-md-5 offset-md-1 mb-4">
      <%= attachment_image_tag @event, :plan_image, class: "farm_pictures", fallback: asset_path('no_images/no_image_md.png'), size: '300x300' %>
    </div>
    <div class="col-12 col-md-5 mr-auto my-2">
      <h4 class="text-left mb-3"><%= @event.title %></h4>
      <h6><i class="fas fa-heart text-danger"></i> <%= @event.event_favorites.count %></h6>
      <%= safe_join(@event.body.split("\n"),tag(:br)) %>
    </div>
  </div>

  <div class="row">
    <div class="col-12 col-md-10 mx-auto mb-2 event">
      <h5 class="mb-2">イベント概要</h5>
      <table class="table">
        <tbody>
          <tr>
            <th>イベント期間</th>
            <td>
              <p>各日の日程詳細や予約状況の確認、日時・予約受付状況を更新する場合、<br>以下のボタンから希望の日程を選択してください：</p>
              <% @schedules.each do |schedule| %>
                <%= link_to farmers_event_schedule_path(schedule), class: "btn btn-success btn-sm mr-3" do %>
                  <%= schedule.date.strftime("%Y/%m/%d") %>
                <% end %>
              <% end %>
              </td>
          </tr>
          <tr>
            <th>開催時刻</th>
            <td><%= @schedule.start_time.strftime("%H:%M") %> ～ <%= @schedule.end_time.strftime("%H:%M") %></td>
          </tr>
          <tr>
            <th>イベント参加費（税込）</th>
            <td><%= @event.fee %></td>
          </tr>
          <tr>
            <th>キャンセル・変更ポリシー</th>
            <td><%= @event.cancel_change %></td>
          </tr>
          <tr>
            <th>そのほか</th>
            <td><%= @event.etc %></td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>

  <div class="row">
    <div class="col-12 col-md-10 mx-auto mb-2 event">
      <%= render "farmers/events/access", event: @event %>
      </table>
      <% unless @schedule.date > Date.today %>
        <div class="form-group d-flex mt-3">
          <%= link_to "編集する", edit_farmers_event_path(@event), class: "btn btn-success mr-3" %>
          <%= link_to "削除する", farmers_event_path(@event), method: :delete, data: { confirm: "このイベントを全日程まとめて削除しますか？"},class: "btn btn-danger" %>
        </div>
      <% end %>
    </div>
  </row>
</div>